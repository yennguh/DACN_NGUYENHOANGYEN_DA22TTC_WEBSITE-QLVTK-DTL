import React from 'react';
import Profile from '../profile/Profile';

export default function AdminProfile() {
    // Reuse the existing Profile page/component so admin sees the same layout
    return <Profile />;
}

